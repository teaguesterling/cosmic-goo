[package]
name = "goo-compose"
version = "0.1.0"
edition = "2021"
description = "Picker-driven sentence builder over goo — v0 (dmenu); libcosmic GUI is v1"
license = "MIT"

[[bin]]
name = "goo-compose"
path = "src/main.rs"

[dependencies]
goo-engine = { path = "../goo-engine" }
serde_json = "1"
