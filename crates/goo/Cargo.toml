[package]
name = "goo"
version = "0.1.0"
edition = "2021"
description = "cosmic-goo CLI — the thin orchestration bin over goo-engine"
license = "MIT"

[[bin]]
name = "goo"
path = "src/main.rs"

[dependencies]
goo-engine = { path = "../goo-engine" }
serde_json = "1"
